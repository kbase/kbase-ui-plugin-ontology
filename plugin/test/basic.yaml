---
description: Accessing Ontology Plugin
cases:
  - description: should load the ontology plugin
    tasks:
      - subtask: login
      - action: navigate
        path: ontology/about
      - subtask: plugin
      - wait: forElement
        selector:
          - type: plugin
            value: ontology
