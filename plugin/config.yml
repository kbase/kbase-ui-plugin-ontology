## Plugin Configuration
---
package:
  name: ontology
  description: A plugin providing landing page support for ontology via the relation engine
  type: iframe
install:
  routes:
    - path: ["ontology", "term", {type: rest, joinWith: "/", name: rest}]
      view: term
      authorization: true
